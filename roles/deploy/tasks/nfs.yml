---

- name: Install NFS tools
  yum:
    name: nfs-utils
    state: latest

- name: Mount NFS shares
  mount:
    fstype: nfs
    opts: hard,bg
    dump: 0
    passno: 0
    state: mounted
    src: "{{ item.src }}"
    path: "{{ item.path }}"
  with_items: "{{ nfsmounts }}"
